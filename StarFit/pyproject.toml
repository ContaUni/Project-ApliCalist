[tool.poetry]
name = "aplicalist"
version = "0.1.0"
description = "Project"
authors = ["vitorguinomio <vitor@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.11"
django = "^5.2.5"
psycopg2-binary = "^2.9.9"
python-dotenv = "^1.0.1"
gunicorn = "^20.1.0"
whitenoise = "^6.9.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"